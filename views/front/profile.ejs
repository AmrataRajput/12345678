<%- include('../templates/header') %>

<section class="dash-inner-sec">
    <div class="container">
        <div class="row">
            <div class="col-lg-4 col-md-4">
               <div class="row align-item-center">
                            <div class="col-lg-12 col-md-12">
                              <div class="uploader-img">
                                <% if(result[0].profile_image!='') { %>
                                     <img id="user_profile_image_preview" src="/upload_user_profile/<%=result[0].profile_image%>" class="m-t-5" >
                                     <% } else { %>
                                     <img id="user_profile_image_preview" src="assets/images/other/9.png" class="m-t-5" >
                                     <% } %>
                                  </div>
                            </div>
                            <div class="col-lg-12 col-md-12">
<!-- 
                                  <div class="input-group mb-3 mb-3 verification">

                                      <input type="text" class="form-control" readonly="" id="text_img_name" data-rule-required="true" data-msg-required="Please select your Image.">
                                      <span class="input-group-btn for-profile">
                                      <span class="btn btn-green btn-file1">
                                      Upload a picture <input type="file" id="imgInp6" value="" name="image" accept="image/*" onchange="readURL(this);">
                                       <input type="hidden" name="old_image" value="">
                                      </span>
                                      </span>
                                   </div>
                                  <span id="imaggg_err" class="error_validate"></span> -->
                                  
                            </div>
                          </div>
                 </div>
             <div class="col-lg-7 col-md-12">
                 <div class="dashboard-box">
                     <div class="create-wallet-block profile-input">
                           <h4 class="green-heading ">Basic Information</h4>
                               <% if(err_msg!=undefined){ %>
                        <span class="error_validate flash_msg"><%= err_msg %></span>
                        <% }%>
                       <% if(success_msg!=undefined){ %>
                      <span class="succ_flash_msg" style="color: green;"><%= success_msg %></span>
                     <% }%>
                     <form method="post" action="/update-profile" id="profile" enctype="multipart/form-data">
                          <div class="form-group  input-dash ">
                              <input class="form-control" type="text" name="first_name" placeholder="First Name" data-rule-required="true"  data-msg-required="Please enter First Name." value="<% if(result[0].first_name !='') { %> <%=result[0].first_name%> <% } %>">
                          </div>
                          <!-- <div class="form-group  input-dash ">
                              <input class="form-control" type="text" name="last_name" placeholder="Last Name" data-rule-required="true"  data-msg-required="Please enter Last Name." value="<% if(result[0].last_name !='') { %> <%=result[0].last_name%> <%} %>">
                          </div> -->
                          <div class="form-group  input-dash ">
                            <input class="form-control" type="text" name="email" placeholder="Email Address" readonly="" value="<% if(result[0].email!='') { %> <%=result[0].email%> <% } %>">
                          </div>
                          <div class="form-group  input-dash">
                             <input class="form-control" type="file" name="image">
                          </div>
                          <% if(result[0].profile_image!='') { %>
                         <!--  <img src="/upload_user_profile/<%=result[0].profile_image %>" height='50' width='50'> -->
                          <input type="hidden" name="old_image_name" value="<% if(result[0].profile_image!='') { %> <%=result[0].profile_image%> <% } %>">
                          <% } %>
                          <div class="text-center mlti-btn">
                              <input type="reset" id="cancel" class="btn btn-common light-green" value="Reset">
                               <input type="submit" value="Save" id="submit-profile" class="btn btn-common light-green">
                          </div>       
                        </div>
                 </div>
             </div>
        </div>
    </div>
</section>

<%- include('../templates/footer') %>

<script src="/assets/js/jquery.validate.min.js"></script>
<script type="text/javascript">
$('#profile').validate({
      onfocusout: function(element) {
      this.element(element);
      },
      errorClass: 'error_validate',
      errorElement:'span',
      highlight: function(element, errorClass) {
      $(element).removeClass(errorClass);
      },
      submitHandler:function(form)
         {
           $('#submit-profile').attr('disabled',true);
           form.submit();
         }
      });
</script> 
<script type="text/javascript">
$(document).ready(function () {
var flash_msg=$('.flash_msg').html();
if(flash_msg!="" && flash_msg!=undefined)
{
  setTimeout(function(){ $('.flash_msg').html('');},4000);
}    
});


$(document).ready(function () {
var succ_flash_msg=$('.succ_flash_msg').html();
if(succ_flash_msg!="" && succ_flash_msg!=undefined)
{
setTimeout(function(){ $('.succ_flash_msg').html('');},4000);
}    
});  

$('#cancel').click(function(){
  location.reload();
});

</script> 




